import React from 'react';
import { View } from 'react-native';
import maintenanceStyles from './maintenanceStyles';

export default (controller) => (
  <View style={maintenanceStyles.maintenanceContainer} />
);
