import React from 'react';
import { View } from 'react-native';
import accommodationStyles from './accommodationStyles';

export default (controller) => (
  <View style={accommodationStyles.accommodationContainer} />
);
